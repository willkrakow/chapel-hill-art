import { Theme } from "@emotion/react"

const theme: Theme = {
    colors: {
        black: '#000',
        primary: '#333',
        background: '#f5f5f5',
        white: '#fff',
        grey: '#ccc',
    }
}

export default theme